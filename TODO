TODO — Projet IHM
Backend (Spring Boot)

    [ ] Feature : Moteur de tarification et validation

        Implémenter la logique de calcul des prix (Prix de base de la zone + caractéristiques/options de l'événement).

        Ajouter la validation stricte de la capacité maximale des "zones debout" (empêcher la surréservation au niveau de la base de données via un verrouillage pessimiste/optimiste).

    [ ] Feature : Gestion des Salles et Configurations

        Créer les endpoints pour gérer les templates de salles (Cinéma, Conférence, Concert avec zones debout/assises).

    [ ] Feature : Système de Réservation & Expiration

        Implémenter un mécanisme de réservation temporaire (ex: panier expire après 10 minutes) pour libérer les places/billets non payés (via @Scheduled ou Redis).

    [ ] Feature : Génération de Tickets & QR Code

        Intégrer une librairie (ex: ZXing) pour générer un QR Code unique par ticket lors de la validation du paiement.

Mobile Flutter (ontik)

    [ ] Fix : payment

        Précision : Gérer proprement les états du cycle de vie (loading, success, failure) et sécuriser la validation côté backend. Assurer un fallback si l'application crash pendant le processing.

    [ ] Fix : template salle (selon les types de salle)

        Précision : Adapter dynamiquement l'UI de sélection. Si type = Debout, afficher une jauge de remplissage/capacité. Si type = Assis/Mixte, afficher une grille interactive (CustomPainter ou LayoutBuilder responsive).

    [ ] Fix : selection des listes imbriquees dans caracteristique genre evenement

        Précision : Remplacer les menus déroulants denses par des puces sélectionnables (FilterChip) ou un système de "Tags" avec recherche (Auto-complete) pour respecter les critères d'ergonomie (charge mentale réduite).

    [ ] Feature : Gestion des réservations utilisateur

        Afficher l'historique des réservations (À venir, Passées, Annulées) avec option de téléchargement du ticket/QR Code hors-ligne.\

    [ ] Fix : bouton retour qui donne page not found

IHM / UX & Wizard (Événementiel)

    [X] Optimisation du Wizard multi-étapes (5 parties)

        Étape 1 (Général) & Étape 2 (Temporel) : Ajouter des validations de cohérence en temps réel (ex: date de fin après date de début) avec messages d'erreur explicites (Critère Bastien & Scapin : Guidage / Feedback immédiat).

        Étape 3 (Tarification) & Étape 4 (Localisation) : Permettre la sauvegarde en brouillon à chaque étape pour éviter la perte de données.

        Étape 5 (Récapitulatif) : Afficher un résumé complet et structuré avant la soumission finale.

    [ ] Gestion des états vides et de chargement (Error & Empty States)

        Ajouter des Shimmers/Squelettes de chargement lors de la récupération des listes d'événements.

Tests

    [ ] Backend : test unitaire service EvenementService

        Création avec caractéristiques + zones debout (Vérifier le mapping et les contraintes d'intégrité).

    [ ] Backend : test unitaire TicketService

        Création ticket zone debout (Vérifier l'incrémentation du compteur de places vendues).

    [ ] Backend : test d'intégration flux complet debout

        Scénario : Création événement -> Réservation place -> Paiement -> Validation ticket.

    [ ] Frontend : tester le rendu du wizard avec différentes catégories

        Précision : Tester la réactivité du layout sur différents écrans (Smartphone vs Tablette) et le comportement des listes imbriquées avec de grands volumes de données.

Sécurité & Nettoyage local

    [ ] Sécurité : Authentification & Autorisations

        Configurer Spring Security avec JWT.

        Sécuriser les routes : Seuls les Admins/Organisateurs peuvent créer des salles/événements ; les Clients peuvent réserver.

    [ ] Ajouter un .gitignore à la racine

        Exclure .env, target/, .dart_tool/, ios/.symlinks/, etc.

    [ ] Nettoyer les fichiers inutilisés

        Supprimer chat.json et db.json.